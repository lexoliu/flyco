/**
 * Placeholder for the generated API client.
 *
 * flyco's REST API is specified by /openapi.json at the repo root, exported
 * from the running control plane (see docs/ARCHITECTURE.md, "Version pins
 * and workarounds"). Once that artifact is stable, a TypeScript client gets
 * generated from it and lands in this directory — request/response types,
 * a typed fetch wrapper, and per-operation functions.
 *
 * Nothing in `src/api/` today is meant to be hand-maintained long term:
 * treat every export here as scaffolding that the generator will replace.
 */
export {};
