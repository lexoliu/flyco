[flyco repo notice]{% if failed %} could not clone `{{ slug }}` into `{{ dir }}/`: {{ error }}. The repository is not part of this session — tell the user it failed, and say why.{% else %} `{{ slug }}` is now cloned at `{{ dir }}/` in the workspace, checked out on its requested branch. It is its own git repository — run git with `git -C {{ dir }}` or from inside the directory.{% endif %}
