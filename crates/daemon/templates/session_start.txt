[flyco machine notice] This session runs on {% include "machine_line.txt" %}.
{% if let Some(minimum) = line.minimum -%}
Booting it already {{ minimum }} — that money is spent whether or not this session uses the time.
{% endif -%}
{% if user_chose -%}
{% include "user_chose_machine.txt" %}
{%- else -%}
Flyco chose this machine automatically as the cheapest curated Linux type big enough to work on, so moving to another one is your call to make when the work needs it.
{%- endif %}
Call `machine_status` for what the machine and the budget are now, and `machine_resize` to move to another type — it restarts the machine, so commit your work first.
