Report this session's compute budget: the limit the user set, what machine time and storage have cost so far, and what is left.

The budget covers compute and storage only — never the tokens your own turns cost. When it is exhausted the session pauses immediately, so a long job on an expensive machine is worth pricing against what remains.
